<main>
  <% if (title) { %>
    <h1><%= title %></h1>
  <% } %>

  <% if (grid) { %>
    <div class="classification-grid">
      <%- grid %>
    </div>
  <% } else { %>
    <p class="notice">No vehicles found.</p>
  <% } %>
</main>
